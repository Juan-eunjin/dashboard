spring:
  application:
    name: dashboard

  datasource:
    # 주소(url) 뒤에 /뒤에는 본인의 실제 DB 이름을 적으세요.
    url: jdbc:mysql://localhost:3306/dashboard?serverTimezone=Asia/Seoul
    username: root
    password: "0000"
    driver-class-name: com.mysql.cj.jdbc.Driver

  session:
    store-type: jdbc
    jdbc:
      initialize-schema: always

mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

jira:
  api:
    url: https://eunjintest.atlassian.net  # 여기에 Jira 주소 입력
    email: eunjinshin97@gmail.com          # Jira 계정 이메일
    token: ${JIRA_TOKEN}

server:
  port: 8080